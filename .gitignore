# Ignore virtual environment directory
.venv/

# Ignore Python bytecode and caches
__pycache__/
*.py[cod]

# Environment variables file
.env

# Node modules and build output (frontend)
node_modules/
dist/

# Media uploads (generated locally, not tracked in Git)
media/
uploads/

# Docker volumes (database data is stored in Docker volumes, not Git)
# Note: Comics and user data will NOT be shared via Git
# Team members should run ./setup-sample-data.sh after first clone

